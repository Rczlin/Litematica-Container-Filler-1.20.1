# Litematica Container Filler (1.20.1)

Litematica Container Filler 的 Minecraft 1.20.1 Fabric 移植版本。在原版功能基础上进行了 **性能优化** 和 **稳定性增强**。

---

## 关于此版本

本 Mod 是从 [Litematica Container Filler](https://github.com/MimicEnzymes/Litematica-Container-Filler) 移植到 **Minecraft 1.20.1 Fabric** 的版本。

相较于原版，本版本在以下方面进行了改进：

- **渲染性能优化**：重构了整个渲染系统，从 VertexBuffer 缓存架构改为即刻模式渲染，解决了 1.20.1 上 3D 相机旋转的问题
- **高亮扫描分帧化**：将高亮扫描拆分为分帧执行（HighlightScanPass），避免单帧性能尖峰
- **高亮渲染缓存**：引入渲染缓存机制，减少不必要的重复渲染，高亮预算可配置
- **PCA 异步队列**：PCA 同步和区域扫描异步化，移除 MiniHUD 反射缓存，提升整体响应速度
- **缓存 TTL 可配置化**：缓存容量和生命周期可自定义，附带 GUI 缓存状态监视
- **精确搬运合并路径**：优化物品搬运路径计算
- **开箱前预检**：填充前检查真实方块状态，避免错误填充
- **UI 超时诊断**：容器数据等待超时自动诊断，便于排查问题
- **玩家交互距离可配置**：可自定义交互距离以适应不同服务器环境
- **分类化调试日志**：结构化调试日志系统，附带性能热点计时
- **PCA 请求队列化**：PCA 请求加入队列并支持超时重试冷却，提高稳定性
- **PCA 兼容性**：PCA 接收器包装，兼容其他使用 PCA 协议的模组

---

## 主要功能

### 投影容器自动填充

- 根据 Litematica 投影中保存的容器 NBT 和槽位布局，自动填充真实容器
- **单次填充**：看向目标容器，按下填充快捷键即可
- **工作模式**：持续扫描附近容器，自动排队并填充
- **Crafter 槽位锁定**：即使材料缺失，仍优先执行必要的槽位锁定
- 支持双倍箱子作为一个整体容器处理
- 支持 Carpet `largeBarrels`（需在配置中手动启用）
- 可配置填充半径、动作延迟、安全延迟和状态保护

### 容器状态高亮

在 3D 世界中渲染容器状态：

- 未填充 / 部分填充 / 已满足 / 过度填充
- 物品错误 / 数据未知 / 容器未放置
- 手动标记完成 / 手动标记需填充

当前渲染器使用半透明体、顶部面板和动画 3D 任务标记。正在填充的容器、排队容器、缺料事件均有独立的动画标记。

高亮颜色、透明度、任务标记、动画帧率、渲染半径和各状态开关均可配置。X 光高亮默认关闭。

### 物品替换

物品替换影响实际填充行为，不仅限于材料列表显示：

- 支持全局替换规则和每投影替换规则（投影规则优先）
- 可视化替换选择器
- 支持精确物品数据（自定义名称、附魔、数据组件）
- 格式：`minecraft:stone->minecraft:cobblestone`
- 支持自定义名称匹配：`minecraft:paper#slot1->minecraft:iron_ingot`
- 替换为 `minecraft:air` 可使填充器忽略该物品

### 材料列表集成

- 仅显示建筑方块 / 仅显示容器物品 / 同时显示
- 从材料列表直接打开替换选择器
- 显示替换标记和提示
- 从正在查看的容器收集缺失材料

### 背包与潜影盒物流

- 从背包中的潜影盒提取所需物品
- 支持 QuickShulker
- 支持 TakeItOut 服务端协议
- 不同颜色潜影盒内容匹配时视为兼容
- 有序物品存放：优先返回源潜影盒或匹配的潜影盒
- 慢速点击包模式（适用于插件服/反作弊服务器）

### 容器工具

独立的看向容器工具：

- **清空容器**：输出可丢弃、移入背包、或先移入背包再丢弃
- **填满容器**：用背包中的物品填满目标容器
- **同步容器**：读取模板容器，同步其他目标容器
- **容器打包**：将目标容器物品打包到背包中的潜影盒
- **收集材料**：从目标容器收集 Litematica 材料列表缺失物品

工具支持切换、关闭、按住重复、同容器冷却和可选工具 HUD。

### 容器过滤器

- 禁用 / 白名单 / 黑名单模式
- 可应用于投影填充、容器工具或两者
- 可视化容器选择器
- 通配符规则：`minecraft:*_shulker_box`
- 自动检测模组容器

### 手动容器标记

手动切换容器状态：

- 自动 / 手动标记完成 / 手动标记需填充

可用于投影范围外的容器。

---

## 使用方法

- 默认配置快捷键：`L + C`
- 默认单次填充快捷键：`V`
- 连续填充：在配置中启用 `工作模式`，绑定快捷键，调整填充半径和动作延迟

---

## 依赖

**必须安装：**

- Fabric Loader
- Fabric API
- [MaLiLib](https://modrinth.com/mod/malilib)
- [Litematica](https://modrinth.com/mod/litematica)

**可选：**

- Mod Menu：配置界面入口
- QuickShulker：快速打开背包中的潜影盒
- [PCA](https://modrinth.com/mod/pca)：无需打开容器即可获取容器数据
- MiniHUD：可读取其容器预览缓存
- TakeItOut：服务端潜影盒提取支持

---

## 注意事项

**服务器风险**：自动容器操作可能被服务器插件或反作弊系统视为异常点击。如有需要，请增大动作延迟或启用慢速点击包模式。

**空投影容器清空**：启用后可能会清除投影中为空容器的物品。请在启用前确认投影内容和范围。

**Carpet 大箱子**：仅在服务器实际启用 Carpet `largeBarrels` 时开启此选项。

---

## 许可证

本项目基于 `LGPL-3.0-only` 许可证。
